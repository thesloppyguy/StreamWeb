import React from "react";
import { Login as LoginPage } from "../organisims";
import loginbackground from "../assets/loginbackground.svg";

const Login = () => {
  return (
    <div className="flex h-screen items-center">
      <LoginPage />
    </div>
  );
};

export default Login;
